function Navbar() {
  return (
    <header className="admin-topbar">
      <div>
        <h3>Welcome back</h3>
        <p>Keep the community updates flowing.</p>
      </div>
      <div className="admin-topbar-actions">
        <span className="admin-badge">Admin</span>
      </div>
    </header>
  );
}

export default Navbar;
